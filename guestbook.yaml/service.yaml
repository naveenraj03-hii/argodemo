apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
spec:
  type: NodePort                   # kops-la browser-la paakka
  selector:
    app: guestbook-ui
  ports:
  - port: 80
    targetPort: 80